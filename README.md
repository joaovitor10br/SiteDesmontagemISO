# ISO Builder Web

Ferramenta web local para extração e customização de imagens ISO Linux.

## Requisitos
- Python 3.10+
- Flask
- 7zip
- Linux

## Como rodar
```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
